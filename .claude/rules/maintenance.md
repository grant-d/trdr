# Maintenance

## General

- Keep dependencies up to date for security

## TypeScript

- Run `yarn outdated` periodically
- Update via `yarn upgrade-interactive`

## Python

- Run `pip list --outdated` periodically
- Update via `pip install --upgrade <pkg>`
- Pin versions in requirements.txt
