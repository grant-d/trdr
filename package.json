{
  "name": "trdr",
  "version": "1.0.0",
  "private": true,
  "description": "Grid trading bot with multi-agent trailing orders",
  "author": "Grant Dickinson <grant@jesanna.com>",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "lint": "yarn workspaces run lint",
    "format": "prettier --write .",
    "clean": "yarn workspaces run clean && rimraf node_modules",
    "tc": "   tsc --noEmit",
    "up": "   git gc && yarn global upgrade && yarn && yarn outdated"
  },
  "devDependencies": {
    "@types/node": "^24.0.8",
    "@typescript-eslint/eslint-plugin": "^8.31.0",
    "@typescript-eslint/parser": "^8.31.0",
    "eslint": "^9.30.0",
    "prettier": "^3.6.2",
    "rimraf": "^5.0.0",
    "task-master-ai": "^0.18.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@coinbase-sample/core-ts": "^0.2.0"
  }
}
